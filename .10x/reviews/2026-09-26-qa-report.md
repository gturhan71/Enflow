# QA Raporu — prod-runtime-pg-pipeline (M1+M2+M3) — 2026-09-26

## Sonuç: KOŞULLU ONAY
Otomatik kapsam güçlü; **Windows servisi ve gerçek LaunchDaemon/systemd doğrulanmadı** (manuel liste: `docs/RELEASE_CHECKLIST.md`).

## Çalıştırılanlar
| Süit | Sonuç | Not |
|---|---|---|
| `pnpm verify` (tsc×2, guard'lar, backend vitest, node:test, backend+frontend build) | ✔ 200+ backend test, 31 node:test | |
| `scripts/ci-postgres.sh` (Postgres 16 + RLS + HTTP + pg_dump + SIGTERM) | ✔ | CI job'u PR'larda ilk kez koşacak |
| **RBAC Playwright** | ✔ **1027/1027** (15,2 dk) | `dev.db` KOPYASINDA, izole backend :3002 + vite :5173; SEND_SECURITY_REPORT kapalı |
| e2e-scenario (oracle) | ✔ 12/12 | izole DB |
| Upgrade uçtan uca (sahte remote + PG16 + RLS) | ✔ | migrator yok→durur; v1→v2; bozuk v3→rollback |
| Gerçek launchd (LaunchAgent) | ✔ | kill -9→0,7 sn; kickstart -k→temiz kapanış; upgrade-tool servisi buldu |
| Wizard `--dry-run` (PG + servis yolu) | ✔ | |

## Bulunan hatalar (bu iş sırasında; hepsi düzeltildi + testli)
| # | Şiddet | Hata | Nasıl bulundu |
|---|---|---|---|
| 1 | **Kritik** | PG+RLS'te login/tenantMiddleware kırık (tembel PrismaPromise ↔ AsyncLocalStorage) — Faz 15 kodu hiç PG'de koşmamıştı | yeni CI postgres job'u, ilk koşu |
| 2 | **Yüksek** | PG STATE yedeği hiç alınmıyordu (`?schema=` libpq'da geçersiz + FORCE RLS pg_dump'ı reddediyor); `catch{}` sessizce yutuyordu | upgrade ön-yedeği tasarlarken |
| 3 | **Yüksek** | Servis ortamında (PATH'te node yok) açılıştaki wiki `spawn('node')` ENOENT → asenkron 'error' → **tüm backend çöker** | gerçek launchd denemesi (crash-loop, runs=6) |
| 4 | Orta | Upgrade sağlık kontrolü eski sürecin yanıtını 'başarılı' sayabiliyordu | uçtan uca upgrade denemesi |
| 5 | Orta | SQLite ön-yedeği `-wal/-shm` içermiyordu | kod incelemesi |
| 6 | Orta | **Kapanış aktif istekleri kesiyordu** (`closeAllConnections` sinyal anında; 500 ms'de ECONNRESET ile gösterildi) — "graceful" iddiası abartılıydı | PR sonrası risk incelemesi → f0a4475: idle kapat + 3 sn grace + zorla; gerçek http testi (eski kodda kırmızı) |
| 7 | Yüksek (geçiş) | Eski upgrade-tool ile ilk kez yükseltilen mevcut kurulumda `dist/` oluşmuyor → `pnpm start` açılmıyor | PR sonrası risk incelemesi → f0a4475: `prestart` otomatik derleme; dist yokken `pnpm start` gerçekte denendi |

## Kapsam dışı / doğrulanmamış (açıkça)
- **Windows/WinSW:** hiç çalıştırılmadı; graceful shutdown (Node Windows'ta SIGTERM almaz) bilinmiyor.
- **systemd:** şablon üretiliyor ve birim testli; `systemd-analyze verify` bu macOS'ta yok, gerçek Linux'ta koşulmadı.
- **LaunchDaemon (sudo) + reboot:** denenmedi (yalnız LaunchAgent).
- **Upgrade `git checkout <tag>` yolu:** deneme deposunda tag yoktu (commit kanalı test edildi).
- CI `postgres` job'u GitHub Actions'ta henüz koşmadı (yerelde aynı script yeşil).
- Yük/dayanıklılık, çoklu-replika, PG bağlantı havuzu altında shutdown testi yok.
